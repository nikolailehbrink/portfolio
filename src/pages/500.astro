---
import ErrorPageLayout from "@/layouts/ErrorPageLayout.astro";

type Props = {
  error: unknown;
};

const { error } = Astro.props;

const message =
  error instanceof Error
    ? error.message
    : "There is an unknown error on this website.";
---

<ErrorPageLayout title="Error" description={message} />
